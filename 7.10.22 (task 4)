#include <iostream>

using namespace std;

int main()
{
    long num, start, end;
    cout << "Enter any two-digit number bigger, than 10." << endl;
    cin >> num;
    start = (num / 10) + (num % 10);
    end = (num / 10) * (num % 10);
    for (int i = start; i <= end; i++) {
        cout << i << " ";
    }
    if (num == 10) {
        cout << "Error";
    }
}
