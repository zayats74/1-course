#include <iostream>

using namespace std;

int main()
{
	int n;
	cin >> n;
	int* ar = new int[n];
	for (int i = 0; i < n; i++) {
		cin >> ar[i];
	}
	for (int i = n - 1; i >= n-3; i--) {
		ar[i] = -1;
	}
	for (int i = 0; i < n; i++) {
		cout << ar[i]<<" ";
	}
}
